services:
  app:
    volumes:
      - type: bind
        source: ${SNS_MEDIA_INSTAGRAM_COOKIE_HOST_FILE:?Set SNS_MEDIA_INSTAGRAM_COOKIE_HOST_FILE to a Netscape cookies.txt file}
        target: /run/secrets/instagram.cookies.txt
        read_only: true
    environment:
      SNS_MEDIA_INSTAGRAM_COOKIE_FILE: /run/secrets/instagram.cookies.txt
